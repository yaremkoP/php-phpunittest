# epam-php-phpunittest

###How to run
* To run a test you have to run `./vendor/bin/phpunit`.
* All tests must have name like `your_nameTest.php`.
###Settings
You can find `phpunit` settings in root directory.
